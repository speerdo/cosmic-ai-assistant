[package]
name = "cosmo-daemon"
description = "The cosmo daemon: the state machine that owns the microphone, hotkey, models, and tools"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true
